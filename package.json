{
  "name": "recall-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Recall MCP Server using HTTP transport",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node api/index.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "@recallnet/agent-toolkit": "^0.0.5",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
